#配置本地的配置中心文件地址和github上的相关信息
spring:
  cloud:
    config:
      uri: http://127.0.0.1:1299  #git文件地址
      label: master #分支
      name: application-plat-service #github上面的名称
      # profile: ${spring.profiles.active} #环境 key :vlaue 在运行的时候命令
      #java -jar -Dspring.profiles.active=dev aigou_plat_service_8001-1.0-SNAPSHOT.jar
      profile: dev #环境